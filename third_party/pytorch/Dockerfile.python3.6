FROM nvidia_cuda_base_image

RUN apt update && apt install -y \
    python3 \
    python3-pip && \
    pip3 install \
        numpy \
        pyyaml && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/* && \
    rm -rf ~/.cache/pip